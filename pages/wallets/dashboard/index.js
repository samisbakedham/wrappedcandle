import { DashboardLayout } from '../../../components/layouts/DashboardLayout';

WalletDashboard.getLayout = (page) => {
    return <DashboardLayout>{page}</DashboardLayout>;
};

export default function WalletDashboard() {
    return <div></div>;
}
